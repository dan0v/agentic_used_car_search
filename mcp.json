{
  "mcpVersion": "1.0.0",
  "id": "dan0v/car_deals_search_mcp",
  "name": "car-deals-search-mcp",
  "displayName": "Car Deals (MCP)",
  "version": "1.0.0",
  "description": "Search used car listings from Cars.com, Autotrader and KBB (US) or Autotrader UK, Motors.co.uk and Cinch (UK), plus UK MOT history checks. MCP-compatible server.",
  "author": "dan0v",
  "homepage": "https://github.com/dan0v/car_deals_search_mcp",
  "icon": "assets/icon.svg",
  "license": "MIT",
  "entry": {
    "command": "node",
    "args": ["src/server.js"]
  },
  "ports": [
    {
      "name": "http",
      "protocol": "http",
      "port": 3000
    }
  ],
  "permissions": [
    "network",
    "filesystem"
  ],
  "tags": ["scraper","cars","mcp","search"],
  "schema": {
    "tool": "search_car_deals",
    "parameters": {
      "make": { "type": "string", "required": true },
      "model": { "type": "string", "required": true },
      "country": { "type": "string", "enum": ["US", "UK"] },
      "zip": { "type": "string" },
      "yearMin": { "type": "integer" },
      "yearMax": { "type": "integer" },
      "priceMax": { "type": "integer" },
      "mileageMax": { "type": "integer" },
      "maxResults": { "type": "integer" },
      "sources": { "type": "array", "items": { "type": "string" } },
      "oneOwner": { "type": "boolean" },
      "noAccidents": { "type": "boolean" },
      "personalUse": { "type": "boolean" }
    }
  },
  "tools": [
    {
      "name": "search_car_deals",
      "description": "Search for car deals across US (Cars.com, Autotrader, KBB) and UK (Autotrader UK, Motors.co.uk, Cinch) sources"
    },
    {
      "name": "get_listing_details",
      "description": "Fetch a single listing's detail page as markdown"
    },
    {
      "name": "check_mot_history",
      "description": "UK only. Check a UK vehicle's MOT history via GOV.UK and surface outstanding defects, advisories, MOT expiry, and safety recalls",
      "parameters": {
        "registration": { "type": "string", "required": true }
      }
    }
  ]
}
