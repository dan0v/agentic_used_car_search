{
  "mcpVersion": "1.0.0",
  "id": "dan0v/agentic_used_car_search",
  "name": "agentic-used-car-search",
  "displayName": "Agentic Used Car Search (MCP)",
  "version": "1.0.0",
  "description": "Search used car listings from Cars.com, Autotrader and KBB (US) or Autotrader UK, Motors.co.uk, Cinch and eBay Motors (UK), plus UK MOT history checks. MCP-compatible server (Python, stdio).",
  "author": "dan0v",
  "homepage": "https://github.com/dan0v/agentic_used_car_search",
  "icon": "assets/icon.svg",
  "license": "MIT",
  "entry": {
    "command": "uvx",
    "args": ["--from", "git+https://github.com/dan0v/agentic_used_car_search", "agentic-used-car-search", "--country", "US"]
  },
  "permissions": [
    "network",
    "filesystem"
  ],
  "tags": ["scraper", "cars", "mcp", "search", "uk", "mot", "agentic"],
  "schema": {
    "tool": "search_car_deals",
    "parameters": {
      "make": { "type": "string" },
      "model": { "type": "string" },
      "country": { "type": "string", "enum": ["US", "UK"] },
      "zip": { "type": "string" },
      "maxDistance": { "type": "integer" },
      "yearMin": { "type": "integer" },
      "yearMax": { "type": "integer" },
      "priceMax": { "type": "integer" },
      "mileageMax": { "type": "integer" },
      "maxResults": { "type": "integer" },
      "sources": { "type": "array", "items": { "type": "string" } },
      "oneOwner": { "type": "boolean" },
      "noAccidents": { "type": "boolean" },
      "personalUse": { "type": "boolean" },
      "transmission": { "type": "string" },
      "drivetrain": { "type": "string" }
    }
  },
  "tools": [
    {
      "name": "search_car_deals",
      "description": "Search for car deals across US (Cars.com, Autotrader, KBB) and UK (Autotrader UK, Motors.co.uk, Cinch, eBay Motors) sources"
    },
    {
      "name": "get_listing_details",
      "description": "Fetch a single listing's detail page as markdown"
    },
    {
      "name": "check_mot_history",
      "description": "UK only. Check a UK vehicle's MOT history via GOV.UK and surface outstanding defects, advisories, MOT expiry, and safety recalls",
      "parameters": {
        "registration": { "type": "string", "required": true }
      }
    }
  ]
}
